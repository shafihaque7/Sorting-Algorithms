#include <stdint.h>

void merge(uint32_t L[], uint32_t R[], uint32_t A[],uint32_t leftSize,uint32_t rightSize);//Merges the L and the R array. Takes in the size for both of the array.
void mergeSort(uint32_t a[], uint32_t);//merge sort sorts those arrays.
